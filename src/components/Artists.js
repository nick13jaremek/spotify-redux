import React, {Component} from 'react';

/*
* This is a placeholder component. Perhaps a common component can be defined to show all: artists, songs and albums on
* the same layout.
* */
export default class Artists extends Component {

  render() {

    return (
      <div>
        Selected Artists
      </div>
    );
  }
}